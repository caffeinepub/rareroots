{
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations (producers, products, orders, live streams, user profiles, and follow system). Updated `useCreateOrUpdateProducer` and `useCreateOrUpdateProduct` to accept and pass all new brand/rarity fields required by the updated backend interface; also added `useFollowProducer`, `useUnfollowProducer`, `useGetFollowerCount`, and `useIsFollowing` hooks.",
  "frontend/src/components/FeaturedProducers.tsx": "Featured producers section component displaying producer cards with live badges; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/LiveSessionCard.tsx": "Live session card component showing producer info, session title, story, and status badge; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/LiveSessionForm.tsx": "Form component for producers to create/schedule live sessions and update status/story; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/ProducerOrderList.tsx": "Order management list for producers with status filtering and inline status updates; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/ProductForm.tsx": "Product creation/editing form with thumbnail upload, voice note recorder, and all product fields. Added `rarityBadge` text input and `rarityCountdownEnd` datetime-local picker; updated `mutateAsync` call to pass all 10 required arguments to the backend.",
  "frontend/src/components/ProductListManager.tsx": "Product list manager for producers with inline edit/delete actions and create form toggle; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/PurchaseModal.tsx": "Purchase confirmation modal with quantity selector, order summary, and order creation mutation; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/VoiceNotePlayer.tsx": "Custom HTML5 audio player component with play/pause, seek bar, and time display for producer voice notes; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/pages/Home.tsx": "Home page with hero banner, search bar, region filter, featured rarities horizontal scroll, live now section, and products grid; all navigation uses registered routes (`/discover`, `/products`, `/live`).",
  "frontend/src/pages/ProductListing.tsx": "Full product listing page using the updated `RegionFilter` (selected/onSelect props) and `ProductCard` (product/accentColor props); no longer passes removed props like `regions`, `producer`, or `hasLiveSession`.",
  "frontend/src/pages/ProductDetail.tsx": "Product detail page using the correct `/products/$productId` route param; converts `product.producerId` (Principal) to string before passing to `useGetProducer`; renders image, badges, price, voice player, and buy/WhatsApp buttons.",
  "frontend/src/pages/ProducerDashboard.tsx": "Producer dashboard with tabs for Profile/Products/Orders/Live; passes required props (`products`, `isLoading`) to `ProductListManager`, `ProducerOrderList`, and `existingStreams` to `LiveSessionForm`; navigation uses registered routes `/brand-setup` and `/dashboard`.",
  "frontend/src/pages/LiveSessions.tsx": "Live sessions listing page showing all streams filterable by status (active/scheduled/ended) with producer info and story cards.",
  "frontend/src/pages/OrderConfirmation.tsx": "Order confirmation page using the correct `/order-confirmation/$orderId` route param; converts `product.producerId` to string for `useGetProducer`; shows success state, order summary, 92% direct payment note, and WhatsApp tracking link.",
  "frontend/src/pages/OrderHistory.tsx": "\"My Rare Collection\" page with stats row, filter chips, and scrollable order cards showing thumbnail, producer name, star rating, status, re-order and rate buttons; login redirect uses `/dashboard` route.",
  "frontend/src/components/ProducerProfileForm.tsx": "Form for creating/editing a producer's basic profile (name, region, bio, photo). Updated `mutateAsync` call to preserve existing brand fields (`brandName`, `brandTagline`, `brandLogoBlob`, `brandColor`, `voiceStoryBlob`) when saving, satisfying the new 9-argument backend signature.",
  "frontend/src/App.tsx": "Main router configuration registering all routes including /splash, /, /products, /products/$productId, /discover, /producers/$producerId, /dashboard, /brand-setup, /orders, /collection, /order-confirmation/$orderId, /live, and legacy paths; wraps app in ThemeProvider and QueryClientProvider.",
  "frontend/src/components/Layout.tsx": "Fixed top header with SamriddhiSrot branding and profile/cart icons, main content area with correct padding, and fixed bottom navigation bar with four tabs (Home/Discover/Orders/Profile) using Earth Brown active color; uses typed `as const` nav items to satisfy router type constraints.",
  "frontend/src/pages/ProducerProfile.tsx": "Public producer profile page using `/producers/$producerId` route param and string-based hook calls; shows producer info, follow button, brand story, voice player, product grid, and WhatsApp/call action buttons.",
  "frontend/src/pages/ProducerBrandSetup.tsx": "Brand setup form page navigating to `/dashboard` (registered route); pre-fills from existing producer data; includes brand name, tagline, logo upload, color picker, and 30-sec voice recorder with save button."
}